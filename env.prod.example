# Production переменные окружения для ClinNexus
# Скопируйте этот файл в .env.prod и заполните реальными значениями
# НЕ коммитьте .env.prod в git!

# ============================================
# База данных
# ============================================
DB_HOST=db
DB_PORT=5432
DB_NAME=clinnexus
DB_USER=clinnexus
# ВАЖНО: Замените на надежный пароль!
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ============================================
# Backend настройки
# ============================================
APP_ENV=production
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=info

# Путь для хранения загруженных файлов (внутри контейнера)
STORAGE_BASE_PATH=/app/data/uploads

# ============================================
# Frontend настройки
# ============================================
NODE_ENV=production
# URL для API (должен указывать на ваш домен или IP)
NEXT_PUBLIC_API_BASE_URL=http://your-domain.com/api
# Или для локального тестирования:
# NEXT_PUBLIC_API_BASE_URL=http://localhost/api

# ============================================
# LLM настройки (опционально)
# ============================================
# Включить secure mode для использования LLM
SECURE_MODE=false

# Провайдер LLM: azure_openai, openai_compatible, local
LLM_PROVIDER=

# URL для LLM API (если используется openai_compatible или local)
LLM_BASE_URL=

# API ключ для LLM
LLM_API_KEY=

# Модель LLM
LLM_MODEL=gpt-4o-mini

# Температура для LLM (0.0 для детерминированных ответов)
LLM_TEMPERATURE=0.0

# Таймаут для LLM запросов (секунды)
LLM_TIMEOUT_SEC=60

# ============================================
# Дополнительные настройки
# ============================================
# Разрешить редактирование section contracts через API (не рекомендуется для production)
ENABLE_CONTRACT_EDITING=false

# Включить отладочные логи для section mapping
MAPPING_DEBUG_LOGS=false

# ============================================
# Безопасность
# ============================================
# ВАЖНО: Используйте надежные пароли!
# Рекомендуется использовать генератор паролей или менеджер паролей
# Минимальная длина пароля БД: 16 символов, включая буквы, цифры и спецсимволы

